<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/main.css"/>
    <title>Lego Collection</title>
</head>
<body>
    <%- include('partials/navbar', { page: '/lego/sets' }) %>

    <div class="container mx-auto p-4">
        <div class="hero bg-base-200 rounded-lg p-6">
            <div class="hero-content text-center">
                <div class="max-w-md text-black">
                    <h1 class="text-5xl font-bold">Collection</h1>
                    <p class="py-6">
                        Choose a theme to narrow down your search using categories such as:
                        <a href="/lego/sets?theme=CITY" class="btn btn-primary btn-xs">CITY</a>
                        <a href="/lego/sets?theme=SPACE" class="btn btn-primary btn-xs">SPACE</a>
                        <a href="/lego/sets?theme=CASTLE" class="btn btn-primary btn-xs">CASTLE</a>
                    </p>
                </div>
            </div>
        </div>

        <br/>

        <div>
            <table class="table table-zebra text-black">
                <thead>
                    <tr>
                        <th>Image</th>
                        <th>Name</th>
                        <th>Theme</th>
                        <th>Year</th>
                        <th>Num Parts</th>
                        <th>Details</th>
                    </tr>
                </thead>
                <tbody>
                    <% sets.forEach(set => { %>
                    <tr>
                        <td><img src="<%= set.img_url %>" alt="<%= set.name %>" class="w-32 h-32 rounded-lg"></td>
                        <td><%= set.name %></td>
                        <td><a href="/lego/sets?theme=<%= set.theme_id %>" class="btn btn-primary"><%= set.theme_id %></a></td>
                        <td><%= set.year %></td>
                        <td><%= set.num_parts %></td>
                        <td><a href="/lego/sets/<%= set.set_num %>" class="btn btn-primary">Details</a></td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>  
    </div>
</body>
</html>